app = "adeptusboticus"

[env]
  POLLING_INTERVAL_MS = "300000"
  DATA_FILE_PATH = "/data/trackers.json"

[[mounts]]
  source = "trackers_data"
  destination = "/data"

# For non-HTTP worker apps, use processes with restart policy
[processes]
  app = "./adeptusboticus-linux-x64"
